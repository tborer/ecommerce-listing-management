"""Web app API (FastAPI) over the listing pipeline: users, encrypted
supplier/marketplace credentials, criteria, discovery runs, and the
candidate review/listing queue. See docs/web-app-plan.md and docs/deploy.md."""
