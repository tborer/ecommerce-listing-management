// Product name, shared by server and client components. SITE_NAME (server
// env) can override it on the public pages; this is the default everywhere.
export const BRAND = "SourceSnap";

// Lightning-bolt mark ("snap"), used by the logo, favicon and social image.
export const BOLT_PATH = "M36 6 14 36h15l-4 22 25-32H35l5-20z";

// Shown under the waitlist email field and stored with each sign-up as the
// record of what the person agreed to. Keep in sync with /privacy.
export const CONSENT_TEXT = `We'll only use your email to contact you about ${BRAND} early access. You can ask us to delete it anytime.`;
